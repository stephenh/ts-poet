version: 2.1

orbs:
  node: circleci/node@7

workflows:
  version: 2.1
  build:
    jobs:
      - build

jobs:
  build:
    docker:
      - image: cimg/node:20.19.5
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: yarn
      - run: yarn test
